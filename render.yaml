services:
  - type: web
    name: top-topye-1
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: LIARA_API
        sync: false
